//
//  TaskExecutor.swift
//  OzonTechPlaygoundTest
//
//  Created by Yerlan Omarov on 07.05.2022.
//

/*
 func main(_ taskExecutor : () -> Void) {
     taskExecutor()
 }

 main(taskA)
 
import Foundation

func taskA() {
    var input = [Int]()
    var i = 0
    let pairCount = Int(readLine() ?? "") ?? 0

    while i < pairCount {
        input = [Int]()
        let array = readLine()?.split(separator: Character(" ")).map(String.init).map(Int.init)
        if array != nil, array?.first != nil, array?.last != nil , array?.first! != nil, array?.last! != nil{
            var j = 0
            while j < array!.count {
                input.append(array![j]!)
                j += 1
            }
        }
        i += 1
        
        var sum = 0
        for item in input {
            sum += item
        }
        print(sum)
    }
}
*/
