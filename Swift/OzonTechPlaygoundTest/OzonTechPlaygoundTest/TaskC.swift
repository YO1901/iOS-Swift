//
//  TaskC.swift
//  OzonTechPlaygoundTest
//
//  Created by Yerlan Omarov on 07.05.2022.
//

/*
import Foundation

 let deltaCount  = Int(readLine() ?? "") ?? 0

 var input = Array(repeating: 0, count: deltaCount)
 var result = Array(repeating: 0, count: deltaCount + 1)

 let array = readLine()?.split(separator: Character(" ")).map(String.init).map(Int.init)
 if array != nil, array?.first != nil, array?.last != nil , array?.first! != nil, array?.last! != nil {
     input = array!.map { $0! }
 }

 var i = 0
 while i < deltaCount {
     result[i + 1] = result[i] + input[i]
     i += 1
 }

 let minElement = result.min()
 if let minElement = minElement, minElement < 0 {
     result = result.map { $0 - minElement }
 }

 i = 0
 while i < deltaCount {
     print(result[i], terminator: " ")
     i += 1
 }
 print(result[i])


*/
