//
//  TaskD.swift
//  OzonTechPlaygoundTest
//
//  Created by Yerlan Omarov on 07.05.2022.
//

import Foundation

/*
 enum MyColor: Int {
     case green
     case yellow
     case grey
 }
 let colorName = ["G", "Y", "."]

 let s = Array(readLine() ?? "")
 let t = Array(readLine() ?? "")
 let length = s.count

 var result = ""
 var intColor = [Int](repeating: MyColor.grey.rawValue, count: length)
 var used = [Character: Int]()

 var i = 0
 while i < length {
     used[s[i], default: 0] += 1
     
     if t[i] == s[i] {
         intColor[i] = MyColor.green.rawValue
         used[t[i]] = used[t[i]] == 1 ? nil : used[t[i]]! - 1
     }
     i += 1
 }

 i = 0
 while i < length {
     if let _ = used[t[i]], intColor[i] != MyColor.green.rawValue {
         intColor[i] = MyColor.yellow.rawValue
         used[t[i]] = used[t[i]] == 1 ? nil : used[t[i]]! - 1
     }
     result.append(colorName[intColor[i]])
     i += 1
 }
 print(result)

*/
